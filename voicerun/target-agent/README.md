# GAUNTLET Target VoiceRun Agent

This VoiceRun project wraps the BE2 target flow.

## Push

```bash
vr push
```

## Runtime env

- `VOICE_SERVICE_BASE_URL` — shared service base URL, defaults to `http://127.0.0.1:8000`
- `TARGET_VOICE` — optional VoiceRun voice name, defaults to `nova`
